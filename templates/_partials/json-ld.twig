{% if entry is defined %}

    {% set data = entry.getJsonLd() %}
    {% if data %}
        <script type="{{ 'application/ld+json' }}">
            {{ data|json_encode|raw }}
        </script>
    {% endif %}

{% endif %}
