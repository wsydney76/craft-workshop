{% set isLivePreview = craft.app.request.queryParam('x-craft-live-preview') ? true : false%}

{{ sprig('_components/embed.twig', {'id': block.id, 'isLivePreview': isLivePreview}, {'s-trigger': 'load'}) }}

{{ sprig.script }}
